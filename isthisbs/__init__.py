"""IsThisBS? — a static fact-check publication built on the public Lenz API.

Every article on the site is one verified claim from the Lenz catalog,
fetched keylessly via the ``lenz-io`` Python SDK and rendered to static HTML.
"""

__version__ = "0.1.0"
